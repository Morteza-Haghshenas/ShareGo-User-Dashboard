import IPromotedLinkList from '../../models/IPromotedLinkList';

interface IPromotedLinkListFormState {
  showDialog: boolean;
  currentList: IPromotedLinkList;
}

export default IPromotedLinkListFormState;