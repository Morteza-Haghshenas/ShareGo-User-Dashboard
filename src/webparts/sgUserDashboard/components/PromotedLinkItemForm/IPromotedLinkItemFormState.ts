import IPromotedLinkItem from '../../models/IPromotedLinkItem';

interface IPromotedLinkItemFormState {
  showDialog: boolean;
  currentItem: IPromotedLinkItem;
}

export default IPromotedLinkItemFormState;